---

- hosts: ci
  remote_user: "{{ vm_user }}"
  become: true
  connection: ssh
  tasks:
    - debug: msg="JENKINS - DO NOTHING until Jenkins deployment fix"
#  roles:
#    - jenkins

- hosts: qi
  remote_user: "{{ vm_user }}"
  become: true
  connection: ssh
  tasks:
    - debug: msg="QI - here goes the QUALIFICATION TOOLS for Performance/Security/Compliance tests"

- hosts: sp
  remote_user: "{{ vm_user }}"
  become: true
  connection: ssh
#  roles:
#    - sp
  tasks:
    - debug: msg="SP - DO NOTHING"


- hosts: ns
  remote_user: "{{ vm_user }}"
  become: true
  connection: ssh
  roles:
    - nginx

