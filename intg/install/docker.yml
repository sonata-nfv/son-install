---
- name: deploy Docker engine to the 'target' machine
  hosts: "{{ target }}"
  become: true
  roles:
  - docker
  tasks:
  - name: create a directory for Docker logging
    file: path=/var/log/sonata state=directory owner=sonata group=sonata mode=0755

