---

- name: SONATA CMUTD v2.0
  hosts: localhost
  become: false
  connection: local
  pre_tasks:
    - local_action: shell date +'%F %T'
      register: ts
    - debug: msg={{ ts.stdout }}

#  roles:
#    - { role: common, become: true } 
#    - { role: openstackclients, become: true } 

- include: "./{{ ops }}/infra.yml"
- include: "./{{ ops }}/apps.yml"

- hosts: localhost
  become: false
  connection: local

  post_tasks:

  - local_action: shell date +'%F %T'
    register: tsend

  - name: Timestamp
    debug: msg="{{ tsend.stdout }}"

