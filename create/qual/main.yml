---

- include: ../infra.yml
- include: ../common.yml

- hosts: cijks
  become: true
  connection: ssh
  pre_tasks:
    - local_action: shell date +'%F %T'
      register: ts
    - debug: msg={{ ts.stdout }}
  roles:
    - jenkins

- hosts: qitools
  become: true
  connection: ssh
  pre_tasks:
    - debug: msg="QUALIFICATION TOOLS for Performance/Security/Compliance tests goes here"
  tasks:
    - command: sleep 2

- hosts: spqual
  become: true
  connection: ssh
  roles:
    - docker
    - sp

- hosts: nfvi
  become: true
  connection: ssh
  roles:
    - firewall
    - nginx
    - letsencrypt
