- hosts: "{{ plat }}"
  become: true
  connection: ssh
  remote_user: "{{ vm_user }}"
  pre_tasks:
    - debug: msg="{{ lookup('pipe', 'date +%Y%m%d-%H%M') }}"
  roles:
    #- common
    - docker-haproxy
    - docker-squid
    - docker-snort
    - docker-openldap
    #- docker-addblk
    - docker-openvpn
    #- docker-pfsense
