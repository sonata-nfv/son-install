---
- name: create instances
  hosts: localhost
  become: false
  pre_tasks:

    - local_action: shell date +'%F %T'
      register: timestamp

    - debug:
        msg: "{{ timestamp.stdout }}"

#  roles:
#    - { role: common, become: true }
#    - { role: openstackclients, become: true }
#    - { role: os-instance }
  tasks: 
    - debug:
        msg: "VM READY"

- name: deploy SW like the SP, vSA, vCDN
  hosts: "{{ app }}"
  become: false
  roles:
    - "{{ app }}"
