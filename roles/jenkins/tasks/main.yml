---
# tasks file for JENKINS

- name: installing JENKINS 
  include: "{{ ansible_distribution_release }}.yml"

- name: starting Jenkins service
  service: name=jenkins state=started


- name: enable FIREWALL mgmt
  include: "fw.yml"
