- name: deploy kube master
  hosts: kubemaster
  become: true
  connection: ssh
  roles:
  - kubemaster

- name: deploy kube nodes
  hosts: kubenodes
  become: true
  connection: ssh
  roles:
  - kubenode


