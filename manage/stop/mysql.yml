---

- name: stopping MySQL database services
  hosts: "{{ target }}"
  become: true
  tasks:

  - name: MySQL  - stopping Docker containers
    docker_container:
      name: son-mysql
      state: stopped
  - wait_for: host=0.0.0.0 port=3306 timeout=30 delay=10 state=drained

